# Gallery

Pre-rendered animations of the filtration on different inputs. Each video is generated by running `persviz render` and committed to `docs/gallery/`.

## Synthetic phantom (default seed)

<video controls width="100%">
  <source src="gallery/synthetic_seed0.mp4" type="video/mp4">
</video>

```bash
persviz render --out docs/gallery/synthetic_seed0.mp4 -q h
```

## Annulus

<video controls width="100%">
  <source src="gallery/annulus.mp4" type="video/mp4">
</video>

A clean test case: a single solid annulus produces exactly one persistent $H_1$ generator, corresponding to the void surrounding the ring.
